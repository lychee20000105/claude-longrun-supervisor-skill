# v0.4.3 - Adaptive Supervision Cadence

Date: 2026-06-19

## Summary

This release makes the long-run supervisor less rigid by replacing fixed 5-minute checks with adaptive check cadence. It also records field lessons from a real mini program bugfix session so future supervisors can avoid noisy status loops and unsafe resume behavior.

## Changes

- Added adaptive status checks: 20 minutes by default, 30 minutes for quiet healthy runs, and 5 minutes only for fresh bugs, failed/empty rounds, audit/drain/final states, or suspected stuck states.
- Added status metadata: `checkPolicy` and `nextSuggestedSupervisorCheckAt`.
- Updated `start_longrun_supervisor.ps1`, `start_decomposed_longrun_supervisor.ps1`, and `watchdog.ps1` with adaptive timing parameters and fixed-mode switches.
- Added field guidance for zero-byte live Claude logs, duration-first stop conditions, round resume numbering, and installed-skill-as-source publishing.
- Updated README usage notes and CHANGELOG entries.

## Validation

- PowerShell parser validation should pass for all bundled `.ps1` files.
- `git diff --check` should pass before pushing the release mirror.
